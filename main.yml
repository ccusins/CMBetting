jobs:
  my_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Inject API Key
        run: |
          echo "const API_KEY = '${{ secrets.API_KEY }}';" > support.js